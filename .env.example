# Base configuration
BASE_URL=https://reqres.in
OAUTH_TOKEN_URL=https://example.com/oauth/token  # replace with real issuer if available
OAUTH_CLIENT_ID=your_client_id
OAUTH_CLIENT_SECRET=your_client_secret
OAUTH_SCOPE=openid profile email
API_KEY=replace_me_if_applicable

# Playwright
PW_WORKERS=4
